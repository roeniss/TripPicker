package com.picker.trip.repository;

import com.picker.trip.domain.UserPreferenceActivity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserPreferenceActivityRepository extends JpaRepository<UserPreferenceActivity , Integer> {
}
