package com.picker.trip.repository;

import com.picker.trip.domain.UserPreferencePartner;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserPreferencePartnerRepository extends JpaRepository<UserPreferencePartner, Integer> {
}
